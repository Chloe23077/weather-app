<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="WeatherApp2.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:WeatherApp2.Pages"
    Shell.FlyoutBehavior="Disabled">

    <TabBar>
        <Tab Title="Home" Icon="home.png">
            <ShellContent Title="Home"
                ContentTemplate="{DataTemplate local:WeatherHomePage}" Route="WeatherHomePage"/>
        </Tab>
        <Tab Title="Detail" Icon="detail.png">
            <ShellContent Title="Detail"
        ContentTemplate="{DataTemplate local:WeatherDetailPage}" Route="WeatherDetailPage"/>
        </Tab>
        <Tab Title="Weekly" Icon="favorite.png">
            <ShellContent Title="Weekly"
                ContentTemplate="{DataTemplate local:WeatherWeeklyPage}" Route="WeatherWeeklyPage"/>
        </Tab>
        <Tab Title="Setting" Icon="setting.png">
            <ShellContent Title="Setting"
                ContentTemplate="{DataTemplate local:WeatherSettingPage}" Route="WeatherSettingPage"/>
        </Tab>
    </TabBar>

</Shell>
